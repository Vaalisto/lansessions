<%= javascript_include_tag "datepicker", "chosen" %>

<%= form_for(@lanparty) do |f| %>
  <% if @lanparty.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@lanparty.errors.count, "error") %> prohibited this lanparty from being saved:</h2>

      <ul>
      <% @lanparty.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= bootstrap_form_for(@lanparty) do |f| %>
    
    <div class="row">      
      <%= f.text_field :partyname, wrapper: { class: 'col-xs-3' } %>
    </div>

    <div class="row">
      <%= f.text_field :address, wrapper: { class: 'col-xs-3' } %>
    </div>

    <div class="row">
    <%= f.text_field :city, wrapper: { class: 'col-xs-3' } %>
    </div>
    <div class="row">
    <%= f.datetime_picker :startdate, wrapper: { class: 'col-xs-3' } %>
    </div>
    <div class="row">
    <%= f.datetime_picker :enddate, wrapper: { class: 'col-xs-3' } %>
    </div>
    <div class="row">
    <%= f.text_area :description, wrapper: { class: 'col-xs-6' } %>
    </div>

    <div class="row">
      <div class="col-xs-6">
        <%= f.collection_select :game_ids, Game.order(:name), :id, :name, { label: "Games" }, { :multiple => true }  %>
      </div>
    </div>
    <%= f.submit "Submit" %>
  <% end %>

<% end %>